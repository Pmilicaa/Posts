import { createArrowIcon } from "./util/helpers";

export const API_URLS = {
  Posts: "posts",
  Users: "users",
  Comments: "comments",
};

export const leftArrowIcon = createArrowIcon(false);
export const rightArrowIcon = createArrowIcon(true);
